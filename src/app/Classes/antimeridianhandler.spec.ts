import { Antimeridianhandler } from './antimeridianhandler';

describe('Antimeridianhandler', () => {
  it('should create an instance', () => {
    expect(new Antimeridianhandler()).toBeTruthy();
  });
});
